<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <a class="navbar-brand" href="/"><img src='<%= image_url 'logo.png' %>' >前行网</a>
        </div>
        <ul class="header-menu horizontal-list">
            <li>
                <a class="header-menu-tab" href="/jobs"><span class="fa fa-handshake-o"></span>马上应征</a>
            </li>
            <li>
                <a class="header-menu-tab" href="#2"><span class="fa fa-comments-o"></span>前行社区</a>
            </li>
        </ul>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
            <% if !current_user %>
              <li><%= link_to("注册", new_user_registration_path, :class => "btn btn-default glyphicon glyphicon-user") %> </li>
              <li><%= link_to("登入", new_user_session_path, :class => "btn btn-default glyphicon glyphicon-log-in") %></li>
            <% else %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <span class="glyphicon glyphicon-user"></span> <%= current_user.email %>
                  <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <% if current_user.admin? %>
                    <li> <%= link_to("后台管理", admin_jobs_path, :class => "btn fa fa-desktop") %> </li>
                  <% end %>
                    <li> <%= link_to("登出", destroy_user_session_path, :class => "btn fa fa-sign-out", method: :delete) %> </li>
                </ul>
              </li>
            <% end %>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
