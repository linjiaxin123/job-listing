<h2> <%= @job.title %> - 履历列表 </h2>

<hr>
<%= render :partial => "resume", :collection => @resumes, :as => :resume %>
<div>
  <%= link_to("back", admin_jobs_path, :class => "btn btn-default") %>
</div>
